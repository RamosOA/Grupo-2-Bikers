<!DOCTYPE html>
<html lang="en">

<%- include('./partial/head.ejs') %>
    <title>Login</title>

    <body>
        <%- include('./partial/header.ejs') %>
            <main class="main">
                <h1 class="ingreso">Ingreso</h1>
                <p class="leyend">Hola! ingresa tus credenciales para acceder a tu cuenta</p>

                <form class="NameUser" action="/login" method="POST">
                    <label for="Usuario"></label>
                    <input type="text" placeholder="Email" name="login_name" value="<%= locals.oldData ? oldData.login_name : null %>"><br>
                    <% if (locals.errors && errors.login_name) { %>
                        <div class="text-danger">
                            <%= errors.login_name.msg %> 
                        </div>
                    <% } %>
                    <label for="Password"></label>
                    <input class="password" type="password" placeholder="Contraseña" name="login_password"><br>
                    
                    <div id="recordarUsuario">
                        <label id="form-check-label">
                        <input type="checkbox" class="form-check-input" name="remember_user">Recordar mi usuario
                    </label>
                    </div>
                    
                    <% if (locals.errors && errors.login_password) { %>
                        <div class="text-danger">
                            <%= errors.login_password.msg %> 
                        </div>
                    <% } %>
                    <button type="submit" value="Enviar" class="Enviar">Ingresar</button>
                </form> 

            </main>
            <%- include('./partial/footer.ejs') %>
    </body>

</html>